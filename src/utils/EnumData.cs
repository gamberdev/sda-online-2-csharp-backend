public enum OrderBy
{
    ASC,
    DESC
}

public enum SortBy
{
    Name,
    Price,
    Date

}

public enum OrderStatus
{
    Pending,
    Processing,
    OutForDelivery,
    Delivered,
    Canceled
}

public enum Role
{
    Customer,
    Admin
}
